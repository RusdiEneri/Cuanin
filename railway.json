{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-interaction --prefer-dist --no-dev --optimize-autoloader && npm install --include=dev && NODE_OPTIONS=--max-old-space-size=1536 npm run build"
  },
  "deploy": {
    "startCommand": "bash start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}